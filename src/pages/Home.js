import { NavLink } from "react-router-dom"

export default function Home() {
    return (
        <div>
            THis is the home component
            <NavLink onCanPlayThrough={ }></NavLink>
        </div>
    )
}